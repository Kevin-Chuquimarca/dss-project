export interface Book {
  isbn: string
  id: number
  title: string
}

export type BookType = {
  isbn: string
  id: number
  title: string
}

export interface Author {
  id: number
  name: string
  lastName: string
}
